{
  "template": {
    "settings": {
      "index": {
        "number_of_shards": "${primary_shard_count}",
        "number_of_replicas": "0",
        "lifecycle": {
          "name": "${lifecycle}"
        }
      }
    }
  },
  "_meta": {
    "description": "Settings for ${name}"
  }
}
