{
  "id": "paywallet",
  "displayName": "Payment Wallet ${env}",
  "indexTemplate": {
    "indexPattern" : "logs-${env_separator}paywallet-*-{prefix}.{env}"
  },
  "dataStream": [
    "logs-${env_separator}paywallet-{prefix}.{env}"
  ],
  "dataView": {
    "indexIdentifier": "${env_separator}paywallet"
  },
  "customComponent": "default"
}
