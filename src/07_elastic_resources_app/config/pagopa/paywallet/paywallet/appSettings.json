{
  "id": "paywallet",
  "displayName": "Payment Wallet ${env}",
  "indexTemplate": {
    "indexPattern" : "logs-paywallet-*-${prefix}.${env}"
  },
  "dataStream": [
    "logs-paywallet-${prefix}.${env}"
  ],
  "dataView": {
    "indexIdentifier": "paywallet"
  },
  "customComponent": "basic-pipeline-lifecycle@custom",
  "ingestPipeline": "convert_responseTime_httpCode"

}
