apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  namespace: ${namespace}
  name: elastic-agent
subjects:
  - kind: ServiceAccount
    name: elastic-agent
    namespace: ${namespace}
roleRef:
  kind: Role
  name: elastic-agent
  apiGroup: rbac.authorization.k8s.io