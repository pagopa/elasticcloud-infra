{
  "id": "pagopapaymentspull",
  "displayName": "Pagamenti Pull ${env}",
  "indexTemplate": {
    "indexPattern" : "logs-${env_separator}gpd-payments-pull-*-{prefix}.{env}"
  },
  "dataStream": [
    "logs-${env_separator}pagopapaymentspull-{prefix}.{env}"
  ],
  "dataView": {
    "indexIdentifier": "${env_separator}gpd-payments-pull"
  },
  "customComponent": "default"
}
