{
  "id": "printit",
  "displayName": "Print It ${env}",
  "indexTemplate": {
    "indexPattern" : "logs-${env_separator}print-payment-*-{prefix}.{env}"
  },
  "dataStream": [
    "logs-${env_separator}print-payment-notice-service-{prefix}.{env}",
    "logs-${env_separator}print-payment-notice-generator-{prefix}.{env}",
    "logs-${env_separator}print-payment-notice-functions-{prefix}.{env}"
  ],
  "dataView": {
    "indexIdentifier": "${env_separator}print-payment"
  },
  "customComponent": "default"
}
